# Write a program to sum a list with 4 numbers.

l = [34, 75, 234, 123]
# l = [int(input("Enter number: ")) for i in range(4)] # list comprehension to take input from user
print(sum (l)) # sum of the list
